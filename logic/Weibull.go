package logic

func Weibull(timeDuration, beta, theta float64) bool{

	//p := beta / theta * math.Pow((timeDuration / theta) ,(beta - 1))
	ifRun := true
	//rand.Seed(time.Now().UnixNano())
	//if rand.Float64() > p{
	//	ifRun = false
	//}

	return ifRun
}
